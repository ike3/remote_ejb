apiVersion: apps/v1
kind: Deployment
metadata:
  name: remote-ejb-transactions-server
  labels:
    app: remote-ejb-transactions-server
    tier: devops
spec:
  selector:
    matchLabels:
      app: remote-ejb-transactions-server
      tier: devops
  replicas: 1
  template: 
    metadata:
      labels:
        app: remote-ejb-transactions-server
        tier: devops
    spec:
      serviceAccountName: remote-ejb-transactions-service-account
      containers:
        - name: remote-ejb-transactions-server
          image: remote-ejb-transactions-server:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8083