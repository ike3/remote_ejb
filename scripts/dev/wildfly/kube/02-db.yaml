apiVersion: apps/v1
kind: Deployment
metadata:
  name: remote-ejb-transactions-db
  labels:
    app: remote-ejb-transactions-db
    tier: devops
spec:
  selector:
    matchLabels:
      app: remote-ejb-transactions-db
      tier: devops
  replicas: 1
  template: 
    metadata:
      labels:
        app: remote-ejb-transactions-db
        tier: devops
    spec:
      serviceAccountName: remote-ejb-transactions-service-account
      containers:
        - name: remote-ejb-transactions-db
          image: remote-ejb-transactions-db:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 1521